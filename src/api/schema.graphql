type Query {
    page: [Page!]!

}
type Page {
    id: ID!
    title: String
    body: [bodyContent!]!
}
type bodyContent {
    value: String
    processed: String
    summary: String
    format: String
}